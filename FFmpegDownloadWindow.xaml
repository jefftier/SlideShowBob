<Window x:Class="SlideShowBob.FFmpegDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FFMPEG Installation" 
        Height="300" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="FFMPEG Required" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <!-- Description -->
        <TextBlock Grid.Row="1" 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,15">
            <Run Text="FFMPEG is required for video thumbnail generation and playback features."/>
            <LineBreak/>
            <Run Text="Would you like to download and install it now?"/>
        </TextBlock>

        <!-- Download Info -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Download size: ~50-100 MB" 
                       FontStyle="Italic" 
                       Foreground="Gray"
                       Margin="0,0,0,5"/>
            <TextBlock x:Name="StatusTextBlock" 
                       Text="Ready to download" 
                       Foreground="DarkBlue"/>
        </StackPanel>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="3" 
                     x:Name="ProgressBar" 
                     Height="20" 
                     Minimum="0" 
                     Maximum="100" 
                     Value="0"
                     Margin="0,0,0,15"
                     Visibility="Collapsed"/>

        <!-- Progress Text -->
        <TextBlock Grid.Row="4" 
                   x:Name="ProgressTextBlock" 
                   Text="" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   FontSize="14"
                   Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="DownloadButton" 
                    Content="Download &amp; Install" 
                    Width="120" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="DownloadButton_Click"/>
            <Button x:Name="CancelButton" 
                    Content="Skip" 
                    Width="80" 
                    Height="30"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

