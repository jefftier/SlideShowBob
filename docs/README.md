# Documentation

This folder holds **maintained** reference material for contributors and AI-assisted work. Older PR notes and one-off analyses were removed from the repo root to reduce noise.

| File | Contents |
|------|----------|
| [ARCHITECTURE.md](./ARCHITECTURE.md) | Where slideshow, GIF, and manifest logic lives |
| [SECURITY.md](./SECURITY.md) | HTTP headers, CSP, File System Access notes, release checklist |

The Vite app lives at the **repository root** (`src/`, `vite.config.ts`, `package.json`). Run `npm run dev` from the root.
